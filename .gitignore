# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
.env

# Editor
.vscode/
.idea/
.DS_Store

# Local artifacts
.playwright-mcp/
*.tmp
*.bak

# Mavis session scratch
.mavis/
*.scratch

# Old v1/v2/v3 development artifacts (kept locally for reference, not in repo)
backend.zip
mockup-default-route.png
mockup-pkxsha.png
v2-*.png
v3-*.png

# NOTE: backend/data.sqlite3 IS committed (GitHub Actions needs it).
# When running locally it's fine to also have it uncommitted; workflow
# reads the committed version, runs scraper, and re-commits the updated one.
